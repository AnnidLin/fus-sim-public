# Python 与 C/C++ 环境安装记录

记录日期：2026-05-13  
项目目录：`D:\AIprogram\fus-sim`

## 1. 已安装的系统工具

### Python

本次新增安装：

| 工具 | 版本 | 安装位置 | 用途 |
|---|---:|---|---|
| Python | 3.12.10 | `D:\Python\Python312\python.exe` | 作为本项目的 k-wave-python 与科学计算运行环境 |

原有环境保留：

| 工具 | 版本 | 安装位置 | 说明 |
|---|---:|---|---|
| Python | 3.14.1 | 已移除/不再迁移 | 原有 Python，不作为 k-Wave 主环境 |

验证命令：

```powershell
py -0p
py -3.12 --version
python --version
```

### Microsoft Visual C++ Build Tools

本次新增安装：

| 工具 | 版本 | 安装位置 | 用途 |
|---|---:|---|---|
| Visual Studio Build Tools 2022 | 17.14.31 | `C:\Program Files (x86)\Microsoft Visual Studio\2022\BuildTools` | 提供 MSVC C/C++ 编译器、链接器和 Windows SDK |
| MSVC C/C++ compiler | 19.44.35226 | `...\VC\Tools\MSVC\14.44.35207\bin\Hostx64\x64\cl.exe` | 编译 Python C/C++ 扩展或后续 C/C++ 程序 |

验证命令：

```powershell
cmd.exe /c 'call "%ProgramFiles(x86)%\Microsoft Visual Studio\2022\BuildTools\Common7\Tools\VsDevCmd.bat" -arch=x64 -host_arch=x64 && cl'
```

验证结果：`cl` 可用，显示 `用于 x64 的 Microsoft (R) C/C++ 优化编译器 19.44.35226 版`。

## 2. 项目虚拟环境

虚拟环境位置：

```text
D:\AIprogram\python-envs\kwave312
```

创建方式：

```powershell
D:\Python\Python312\python.exe -m venv D:\AIprogram\python-envs\kwave312
```

后续项目运行建议统一使用：

```powershell
D:\AIprogram\python-envs\kwave312\Scripts\python.exe
```

示例：

```powershell
D:\AIprogram\python-envs\kwave312\Scripts\python.exe simulate_spherical_focus.py
```

说明：

- Python 主程序已迁移为 D 盘安装：`D:\Python\Python312\python.exe`。
- 科学计算依赖和项目运行环境安装在 D 盘：`D:\AIprogram\python-envs\kwave312`。
- 后续仿真工作优先使用 D 盘虚拟环境，避免污染系统 Python。

## 3. 虚拟环境中的 Python 包

核心包：

| 包 | 版本 | 用途 |
|---|---:|---|
| k-Wave-python | 0.6.1 | Python 版 k-Wave 仿真接口 |
| numpy | 2.2.6 | 数值数组计算 |
| scipy | 1.15.3 | 科学计算 |
| matplotlib | 3.10.7 | 绘图与结果可视化 |
| h5py | 3.15.1 | HDF5 文件读写 |
| opencv-python | 4.13.0.92 | 图像处理依赖，k-wave-python 自动安装 |
| pillow | 12.2.0 | 图像保存与处理 |

构建与辅助包：

| 包 | 版本 |
|---|---:|
| pip | 26.1.2 |
| setuptools | 82.0.1 |
| wheel | 0.47.0 |
| packaging | 26.2 |
| tqdm | 4.67.3 |
| beartype | 0.22.9 |
| deepdiff | 8.6.2 |
| jaxtyping | 0.3.2 |

完整包列表可用以下命令查看：

```powershell
D:\AIprogram\python-envs\kwave312\Scripts\python.exe -m pip list
```

## 4. 验证记录

### 科学计算与 k-Wave 导入验证

命令：

```powershell
New-Item -ItemType Directory -Force -Path .tmp, outputs\mplconfig-venv312 | Out-Null
$env:TEMP=(Resolve-Path .tmp).Path
$env:TMP=(Resolve-Path .tmp).Path
$env:MPLCONFIGDIR=(Resolve-Path outputs\mplconfig-venv312).Path
D:\AIprogram\python-envs\kwave312\Scripts\python.exe -c "import numpy, scipy, matplotlib, h5py; import kwave; print('ok')"
```

结果：

```text
ok
```

### 项目脚本验证

命令：

```powershell
D:\AIprogram\python-envs\kwave312\Scripts\python.exe simulate_spherical_focus.py
```

结果：脚本成功运行，并生成/更新以下文件：

```text
outputs\spherical_focus\pressure_field.png
outputs\spherical_focus\pressure_field_mpa.csv
outputs\spherical_focus\summary.txt
```

## 5. 安装过程中遇到的问题与处理

1. Python 3.14 下安装 k-wave-python 时，依赖版本回退导致 NumPy 需要本机编译，失败原因是当时没有 C/C++ 编译器。
2. 已改用 Python 3.12.10，并安装 MSVC Build Tools，解决了科学计算包兼容性与编译器问题。
3. 创建虚拟环境时，`ensurepip` 写入用户 Temp 目录被拒绝；已通过将临时目录指向 `D:\AIprogram\fus-sim\.tmp` 解决。
4. 第一次安装依赖时，`opencv-python` 下载中途断流；已使用 pip 重试参数继续安装成功。
5. 第一次导入 `kwave` 时需要额外下载 k-Wave 平台二进制文件；已联网完成，最终 `import kwave` 验证通过。

## 6. 后续使用建议

每次在本项目中运行 Python，优先使用：

```powershell
D:\AIprogram\python-envs\kwave312\Scripts\python.exe
```

如果需要 C/C++ 编译环境，先进入 VS 开发者命令环境：

```powershell
cmd.exe /c 'call "%ProgramFiles(x86)%\Microsoft Visual Studio\2022\BuildTools\Common7\Tools\VsDevCmd.bat" -arch=x64 -host_arch=x64 && cl'
```

后续若要安装新的 Python 包：

```powershell
D:\AIprogram\python-envs\kwave312\Scripts\python.exe -m pip install 包名
```

