# 经颅聚焦超声仿真平台搭建 — 任务总结

## 一、任务背景

师姐（俞佳慧）与范老师沟通后，交代了以下两个任务：

1. **搭建仿真模拟平台**：查找相关文献和源代码，尝试复现已有论文中的仿真系统，为课题组建立自己的仿真平台。
2. **撰写综述**：平台搭建完成后，围绕该研究方向写一篇文献综述。

---

## 二、核心参考论文

**《经颅聚焦超声调控系统的参数仿真研究》**
- 作者：高鹏皓（沈阳工业大学，2022年硕士论文）
- 指导教师：李春胜 副教授

### 论文做了什么？

该论文用 **k-Wave 工具箱（基于MATLAB）** 对经颅聚焦超声（tFUS）进行数值仿真，分为两大部分：

#### 第一部分：声压仿真（换能器定位）
- 使用CT图像构建三维颅骨模型
- 模拟超声换能器放在颅骨外不同位置时的声压分布
- 目标区域：颞叶海马区（用于颞叶癫痫治疗研究）
- 找到峰值声压最强的位置，作为最优换能器放置点

#### 第二部分：热效应仿真（安全性验证）
- 基于声压仿真结果，模拟超声照射时的温度变化
- 验证温度不超过42°C的组织变性点
- 证明所选参数下超声刺激的安全性

### 关键仿真参数

| 参数 | 数值 |
|------|------|
| 基频 | 500 kHz |
| 激励声压 | 1 MPa |
| 占空比 | 6% |
| PRF | 300 Hz |
| 超声刺激持续时间 | 67 ms |
| 超声间隔 | 2.5 s |
| 换能器曲率半径 | 30 mm |
| 换能器直径 | 25 mm |

---

## 三、复现思路（由简到难）

```
第一步：跑通球模型（最简单，论文第2章）
    ↓
第二步：二维颅骨模型声压仿真
    ↓
第三步：三维颅骨模型声压仿真（换能器定位）
    ↓
第四步：热效应仿真（安全性分析）
```

---

## 四、软件选型

### 原论文使用
- MATLAB + k-Wave 工具箱
- GPU：Quadro RTX 4000（用于大规模三维仿真，耗时12-33小时）

### 推荐替代方案（免费）
- **Python + k-wave-python**
- 完全免费，功能与MATLAB版k-Wave基本一致
- 适合入门复现，小模型在普通电脑上可运行

---

## 五、硬件情况

| 配置 | 情况 | 评估 |
|------|------|------|
| CPU | 3.91 GHz | ✅ 够用 |
| 内存 | 31.5 GB | ✅ 良好 |
| GPU | Intel Arc 130T（集成显卡） | ⚠️ 无CUDA，不支持GPU加速 |
| 硬盘 | SSD NVMe | ✅ 够用 |

> **结论**：电脑跑小网格、二维模型没有问题；复杂的三维高精度模型耗时会较长，但复现阶段不需要跑最大模型。

---

## 六、当前待完成的下一步

- [x] 建立 Codex / Claude 接力制协作协议：见 `RELAY_PROTOCOL.md`、`ACTIVE_WORK.md`、`HANDOFF_LOG.md`；规定每步完成后必须同步进度文档
- [x] 建立 Codex / Claude 协同交接文档：见 `HANDOFF_PROJECT_SNAPSHOT.md`、`HANDOFF_CODEX.md`、`HANDOFF_CLAUDE.md`
- [x] 检查Python环境：当前保留 Python 3.14.1，并新增 Python 3.12.10
- [x] 安装 k-wave-python 工具包：已在 `D:\AIprogram\python-envs\kwave312` 虚拟环境中安装并验证
- [x] 跑通最简单的聚焦声场模型仿真：见 `simulate_spherical_focus.py` 和 `outputs/spherical_focus/`
- [x] 搭建二维 k-Wave 时间域声压仿真基线：见 `simulate_kwave_2d_focus.py` 和 `outputs/kwave_2d_focus/`
- [x] 搭建三维声学介质模型层：见 `build_3d_acoustic_model.py` 和 `outputs/skull_model_3d/`
- [x] 将三维介质模型接入小尺寸 3D k-Wave 声压仿真：见 `simulate_kwave_3d_focus.py` 和 `outputs/kwave_3d_focus/`
- [x] 增加三维聚焦结果评估指标：见 `analyze_kwave_3d_focus.py`、`axis_profile.*`、`focus_metrics.*`
- [x] 建立 CT/DICOM 到三维声学模型的转换入口：见 `build_ct_acoustic_model.py`
- [x] 用真实 CT/NIfTI 数据替换当前程序化三维颅骨占位模型并运行 quick 3D 声压仿真：见 `data/raw_ct/079.nii`、`outputs/ct_acoustic_model_3d/`、`outputs/kwave_3d_focus_ct_079/`
- [x] 为真实 CT 病例增加目标点和换能器入射位置选择工具：见 `plan_ct_target_entry.py` 和 `outputs/ct_entry_plan_079/`
- [x] 对 079 生成多入射位置候选并按几何/预计耗时排序：见 `scan_ct_entry_positions.py` 和 `outputs/ct_entry_scan_079/ranked_candidates.csv`
- [x] 对排序第一的 `candidate_012` 做自动传播时间校正后的 k-Wave fast 验证：目标窗口峰值约 `0.549 MPa`，见 `outputs/ct_entry_scan_079/candidate_012/kwave_fast/`
- [x] 对几何排序前三候选做断点续跑式 k-Wave fast 验证：`candidate_011` 目标窗口峰值约 `0.634 MPa`
- [x] 增加目标点候选选择：当前保留 `target_020=[93,121,63]`，见 `select_ct_target_candidates.py`
- [x] 修正源面安全距离：`source_standoff_mm=16` 时 source mask 完全位于背景，见 `check_source_standoff_safety.py`
- [x] 完成 079 当前 quick 3D 最佳声压配置：`target_020 + entry offset=(10,0) + aperture=30 mm + radius=35 mm + cycles=8 + sim-time=55 us`，目标窗口峰值约 `2.254 MPa`
- [x] 增加一阶温升估算入口：见 `estimate_temperature_rise.py` 和 `outputs/thermal_estimate_target_020_best/`；1 秒等效连续声照射下目标点约 `1.45 C`，全局最大约 `23.76 C`
- [x] 增加轻量 Pennes 生物热估算：见 `simulate_pennes_bioheat.py` 和 `outputs/pennes_bioheat_target_020_best/`；按论文式 `67 ms`、`6%` 占空比估算目标点约 `0.006 C`，全局最大约 `0.094 C`
- [x] 加入显式脉冲时序与多刺激周期热复核：`300 Hz`、`6%` 占空比、单 train 目标点约 `0.006 C`，3 train 目标点约 `0.018 C`、全局最大约 `0.175 C`
- [x] 完成材料与灌注敏感性扫描：见 `scan_pennes_sensitivity.py` 和 `outputs/pennes_sensitivity_target_020_best/`；12 组参数全部完成，最保守平均热源组合最高温约 `41.63 C`，低于 `42 C` 但裕量仅约 `0.37 C`
- [x] 修正热模型协议平均占空比：新增 `protocol-averaged` 模式，3 train 有效占空比约 `0.242%`；默认参数下目标点约 `0.016 C`、全局最大约 `0.155 C`，12 组敏感性扫描最坏最高温约 `37.19 C`
- [x] 完成协议重复次数热安全扫描：见 `scan_pennes_protocol_dose.py` 和 `outputs/pennes_protocol_dose_target_020/`；保守材料下扫描 `1-120` trains，最高温约 `37.87 C`，距离 `42 C` 约 `4.13 C`
- [x] 生成 079 病例仿真质量控制报告：见 `generate_case_report.py` 和 `outputs/case_report_079/case_report_079.md`；汇总 CT 分割、当前 best 声压、热安全趋势和模型限制
- [x] 完成 CT 分割阈值敏感性评估：见 `evaluate_ct_segmentation.py` 和 `outputs/ct_segmentation_eval_079/`；当前路径下 `250/300 HU` 保留骨穿越，`400 HU` 以上会丢失入射路径骨穿越，推荐单独尝试 `250 HU` 重建
- [x] 完成 `250 HU` 候选模型 quick 声场对照：见 `outputs/ct_pressure_compare_bone_250/`；源面仍安全，但目标窗口峰值从 `2.254 MPa` 降至 `1.645 MPa`，有效峰值距离未改善，当前 best 仍保留 `300 HU`
- [x] 建立多病例接入清单与单病例 QC 骨架：见 `inventory_ct_cases.py`、`validate_ct_case.py`、`data/processed/ct_case_manifest.*` 和 `outputs/ct_case_qc/`；当前识别真实病例 `079.nii` 与 synthetic DICOM 测试集
- [x] 建立批量 CT 声学模型转换骨架：见 `batch_build_ct_models.py` 和 `outputs/ct_acoustic_models_batch/`；已将 `079` 与 `synthetic_case` 转成独立模型目录，未覆盖当前 best 模型
- [x] 建立单病例 quick 声场复用准备包：见 `prepare_case_quick_pressure.py` 和 `outputs/case_quick_pressure_plan/`；`079` 已生成安全 entry plan 与可复制的 smoke-run 命令，synthetic DICOM 被标记为几何 warning
- [x] 完成 `079` 多病例复用 smoke k-Wave：见 `outputs/case_quick_pressure_runs/079_smoke/` 和 `summarize_quick_pressure_runs.py`；链路闭环成功，但默认 batch target 未调参，目标窗口峰值仅约 `0.013 MPa`
- [x] 建立病例级 target/entry 调参复用骨架：见 `prepare_case_refinement_plan.py` 和 `outputs/case_refinement_plan/`；`079` 自动找回 `target_020=[93,121,63]`，并生成 source-safe 的推荐验证命令
- [x] 完成 `079 candidate_006 (10,0)` refinement 单条验证：见 `outputs/case_refinement_runs/079_candidate_006_offset_10_0/` 和 `summarize_refinement_runs.py`；目标窗口峰值 `2.254 MPa`，约为 smoke baseline 的 `169x`，复现手工 tuned best
- [x] 生成多病例阶段报告：见 `generate_multicase_stage_report.py` 和 `outputs/multicase_stage_report/`；报告明确当前只有真实病例 `079`，`synthetic_case` 仅作 DICOM reader 测试，当前结果不是论文级复现或医学安全结论
- [x] 建立公开 CT 数据源候选清单与 dry-run 下载骨架：见 `catalog_public_ct_sources.py`、`plan_public_ct_download.py`、`data/processed/public_ct_sources.*` 和 `outputs/public_ct_download_plan/`；本轮未下载数据，也未写入 `data/raw_ct`
- [x] 生成公开 CT 病例 shortlist：见 `shortlist_public_ct_cases.py` 和 `outputs/public_ct_case_shortlist/`；当前第一推荐目标为 TCIA Head-Neck Cetuximab 的单个 CT series（patient `0522c0027`、series `5577`），仍需人工选择下载，禁止批量拉取完整集合
- [x] 建立公开病例下载后接入守门脚本：见 `prepare_public_case_import.py` 和 `outputs/public_case_import/hn_cetuximab_0522c0027_series5577/`；当前状态为 `pending_manual_download`，不会下载或创建 `data/raw_ct/public`，后续 public manifest 独立写入 `data/processed/public_import/` 以避免覆盖现有清单
- [x] 生成 TCIA 单 series 手动下载指南：见 `write_tcia_download_guide.py` 和 `outputs/public_tcia_download_guide/`；指南只针对 `0522c0027 / 2000-05-17 / series 5577`，明确禁止完整集合下载，`post_download_commands.ps1` 仅包含本地检查与接入命令
- [x] 复核 GC/Gen3 下载 manifest CSV：见 `review_public_gc_manifest.py` 和 `outputs/public_gc_manifest_review/`；当前 CSV 只包含一个 CT DICOM zip `1.3.6.1.4.1.22213.2.26564.2.zip`，约 `31.565 MB`，Participant ID 为 `0522c0027`，但 `Study Access=Controlled` 可能需要授权
- [x] 建立公开 DICOM zip 本地接入守门脚本：见 `prepare_public_zip_import.py` 和 `outputs/public_zip_import/`；当前状态为 `pending_zip_download`，默认期望 zip 放在 `data/raw_ct/public_downloads/`，只有显式 `--extract` 才会解压到 `data/raw_ct/public/hn_cetuximab_0522c0027_series5577/`
- [x] 改走 Visible Human Head CT 公开数据路线并完成本地接入：见 `prepare_visible_human_zip_import.py` 和 `outputs/visible_human_zip_import/`；`dataverse_files.zip` 被拆分为 `visible_human_female_head_1mm`（234 张）和 `visible_human_male_head_1mm`（245 张），两个病例 QC 均为 `ok`、spacing 均为 `1 mm`
- [x] 完成 Visible Human 两个公开病例的 CT 声学模型转换：见 `outputs/ct_acoustic_models_public/`；quick pressure 准备包见 `outputs/case_quick_pressure_plan_public/`，但默认 source mask 有软组织重叠 warning，下一步需先做源面安全调参
- [x] 完成 Visible Human 源面安全扫描与单例 quick smoke：见 `scan_visible_human_safe_entries.py`、`outputs/visible_human_source_safety_scan/` 和 `outputs/visible_human_quick_smoke_runs/`；当前运行 `visible_human_female_head_1mm / candidate_031`，source mask 全背景 `{0:97}`，目标窗口峰值约 `0.0035 MPa`，有效峰值距目标约 `97.7 mm`，说明公开病例链路已闭环但尚未声场调优
- [x] 完成 Visible Human 安全候选传播时间窗复核：`candidate_031` 从 `45 us` 延长到 `85 us` 后指标基本不变，`target_window_peak_mpa≈0.0035`、`effective_peak_to_target_distance_mm≈97.7`，说明目标区偏弱不是时间窗截断导致，下一步应做 target/entry 或 aperture/radius 调参
- [x] 完成 Visible Human female 第一轮 target refinement 验证：自动目标候选 `target_037=[110,122,40]` 在 `standoff=32 mm`、`entry offset=(0,10) mm` 下 source mask 全背景 `{0:95}`，但 quick 结果目标窗口峰值约 `0.0029 MPa`，有效峰值距目标约 `77.1 mm`；说明安全 refinement 流程可运行，但该目标/入射组合仍未形成有效聚焦
- [x] 完成 Visible Human female 多目标安全筛选与单候选验证：新增 `scan_visible_human_target_refinement.py`，筛选 `target_055/074/040/004/028` 后验证 `target_074=[118,122,48]`、`standoff=40 mm`、`entry offset=(0,10) mm`；source mask 全背景 `{0:95}`，目标窗口峰值提升到约 `0.0092 MPa`，但有效峰值距目标约 `111.9 mm`，且 `65 us` 时间窗短于约 `81.8 us` 传播估计，因此仍只是 quick 候选验证，不是聚焦成功
- [x] 完成 Visible Human `target_074` 的 `90 us` 时间窗复核：`90 us` 与 `65 us` 指标完全一致，`target_window_peak_mpa≈0.0092`、`effective_peak_to_target_distance_mm≈111.9`，说明该候选弱聚焦不是时间窗截断导致，下一步应转向更短 source-to-target 候选或 aperture/入射方向调参
- [x] 完成 Visible Human 更短路径候选 `target_040_s28_y0_z0` 验证：source mask 全背景 `{0:97}`，目标窗口峰值约 `0.0030 MPa`，有效峰值距目标约 `79.1 mm`；该候选有效峰值距离明显优于 `target_074` 但未超过 `target_037`，目标窗口声压也低于 `target_074`，说明继续盲跑同类左侧目标点收益有限
- [x] 按新版 roadmap 接入论文/公开代码参数证据层：新增 `generate_platform_parameter_profile.py`，输出 `outputs/parameter_profiles/platform_parameter_profile.json`、`platform_parameter_source_report.md`、`unsupported_or_uncertain_parameters.md`；明确 `500 kHz` 是 Gao baseline、`1 MPa` 是 source pressure、`ap30/r35` 是当前 tuned 参数、Gao pulse protocol 不是通用 tFUS 协议，当前 quick 结果不是论文级复现
- [x] 完成自由场/水槽换能器 quick 校准层：新增 `simulate_freefield_transducer.py` 和 `compare_freefield_calibration.py`，完成 Gao baseline `ap25/r30/f500` 与当前 079 tuned `ap30/r35/f500` 的均匀水介质对照；输出见 `outputs/freefield_calibration/`，中文报告明确 source pressure、free-field pressure、经颅 in-situ pressure 不能混用
- [x] 接入证据驱动开发层：新增 `EVIDENCE_DRIVEN_DEVELOPMENT.md` 和 `generate_module_evidence_brief.py`，支持 `freefield_calibration`、`ct_hu_mapping`、`thermal_safety`、`entry_planning`、`kwave_simulation_quality`、`multicase_pipeline`；已生成第一份 `outputs/evidence_briefs/freefield_calibration/evidence_brief.md/json`
- [x] 完成 CT-HU 声学映射 profile 化：新增 `acoustic_mapping_profiles/`、扩展 `build_ct_acoustic_model.py --mapping-profile`、新增 `compare_ct_mapping_profiles.py`；已生成 `ct_hu_mapping` evidence brief，并用 `simple_hu300/simple_hu250` 独立重建 079 模型，未覆盖旧 best；对比报告见 `outputs/ct_mapping_profile_compare_079/`
- [x] 加固证据驱动机制：新增真实根目录 `AGENTS.md`，在 `EVIDENCE_DRIVEN_DEVELOPMENT.md` 中明确区分执行前 `evidence_brief` 和执行后 `gap_feedback`，并生成 `outputs/evidence_briefs/kwave_simulation_quality/evidence_brief.md/json`；该 brief 明确 quick/standard/paper-grade 分层、PPW/PML/CFL/网格收敛/runtime/backend/memory 记录要求和下一步 `simulation_presets.json`
- [x] 实现 simulation preset 与 k-Wave 质量 metadata dry-run：新增 `simulation_presets.json` 和 `simulation_quality.py`，为 `simulate_kwave_3d_focus.py`、`simulate_freefield_transducer.py` 增加 `--preset` 与 `--dry-run-quality`；已生成 079 candidate_006 与 freefield ap25/r30 的 dry-run summary，均未运行 k-Wave、未生成压力场
- [x] 完成自由场 `standard` sanity run：见 `outputs/freefield_standard_sanity/ap25_r30_f500_standard/`；Gao baseline `ap25/r30/f500/source=1 MPa`，均匀水介质，runtime 约 `137 s`，`simulation_quality.quality_level=standard`、`is_paper_grade=false`，有效峰值约 `6.168 MPa`，距几何焦点约 `7.0 mm`
- [x] 建立 k-Wave 稳定运行封装：新增 `kwave_runner_health` evidence brief、`kwave_run_healthcheck.py` 和 `run_kwave_command.py`；健康检查与 runner plan 均已生成，默认 `execute=false`，未运行新 k-Wave、未生成压力场
- [x] 建立自由场网格收敛 dry-run 计划层：新增 `freefield_grid_convergence` evidence brief 和 `plan_freefield_grid_convergence.py`；已比较 `dx=1.0/0.75/0.5 mm` 的 PPW、grid、nt、memory 和相对工作量，推荐下一次最多单跑 `dx=0.75 mm`，本阶段未运行 k-Wave、未生成压力场
- [x] 完成 `dx=0.75 mm` 自由场 finer-grid sanity：通过 `run_kwave_command.py --execute` 只运行一个 Gao baseline 水介质自由场标准仿真，输出见 `outputs/freefield_grid_convergence_runs/ap25_r30_f500_dx0.75/`；PPW 约 `3.947`，runtime 约 `854.6 s`，effective peak 约 `5.719 MPa`，target-window peak 约 `5.425 MPa`，与 `dx=1.0 mm` 比较焦点距离只变化约 `0.5 mm`，但 runtime ratio 约 `6.24x`，仍不是 paper-grade
- [x] 建立自由场 PML/边界 dry-run 计划层：新增 `freefield_pml_boundary_review` evidence brief 和 `plan_freefield_pml_boundary_review.py`；固定 `dx=0.75 mm` 比较 `pml=8/12/16`，推荐下一次最多单跑 `pml=12`，PML 厚度约 `9.0 mm`、相对工作量约 `1.04x`，本阶段未运行 k-Wave、未生成压力场
- [x] 完成 `dx=0.75 mm, pml=12` 自由场边界 sanity：通过 `run_kwave_command.py --execute` 只运行一个 PML 候选，输出见 `outputs/freefield_pml_boundary_runs/ap25_r30_f500_dx0.75_pml12/`；与 `pml=8` 相比 effective peak 变化约 `0.00018%`、target-window peak 变化约 `0.00054%`、焦点距离和 FWHM 无变化，说明当前小型自由场模型无明显 PML 敏感性信号，但仍不是 paper-grade
- [x] 生成自由场校准质量阶段报告：见 `generate_freefield_quality_report.py` 和 `outputs/freefield_quality_report/`；报告汇总 `dx=1.0/0.75`、`pml=8/12`、source/free-field/in-situ 边界和剩余限制，结论为自由场阶段可暂时收口，下一步回到 079 CT standard-review 规划
- [x] 生成 079 CT standard-review 计划包：新增 `ct_standard_review` evidence brief 和 `prepare_ct_standard_review_plan.py`；输出见 `outputs/ct_standard_review_plan/079_candidate_006/`，本轮只做 `--dry-run-quality`，未运行 CT k-Wave、未生成压力场；dry-run 为 `preset=standard`、`is_paper_grade=false`、PPW 约 `3.00`、grid `66x45x35`，并生成未来最多单条 runner 命令
- [ ] 下一阶段主线：`参数证据层 -> 自由场/水槽换能器校准 -> CT-HU mapping profile -> simulation preset 分层 -> 病例级复现包`
- [ ] 进一步细化 Pennes 热模型：边界条件、材料参数、真实协议重复次数上限和灌注参数标定
- [ ] 查找公开颅骨CT数据集

---

## 七、文献检索建议

检索关键词：
- `transcranial focused ultrasound simulation`
- `k-Wave skull model`
- `tFUS neuromodulation`
- `经颅聚焦超声 仿真`
- `低强度聚焦超声 神经调控`

推荐检索平台：
- 知网（中文文献）
- PubMed（英文文献）
- Google Scholar

---

## 2026-05-23 k-Wave alpha_power 接口审计

- [x] 生成 `kwave_alpha_power_interface_audit` evidence brief：见 `outputs/evidence_briefs/kwave_alpha_power_interface_audit/evidence_brief.md/json`
- [x] 新增只读审计脚本：`audit_kwave_alpha_power_interface.py`
- [x] 生成接口审计报告：见 `outputs/kwave_alpha_power_interface_audit/interface_audit_report.md/json`

关键结论：
- `simulate_kwave_3d_focus.py` 已经读取 `.npz` 中的 `alpha_power`，并传入 `kWaveMedium`。
- 本地 k-wave-python 源码确认 `alpha_coeff` 接口语义为 `dB/(MHz^y cm)`，solver 路径使用 `alpha_power`。
- `prestus_marsac_mueller_draft` 仍不允许进入 pressure simulation，因为 `alpha_power=1.0` 还需要 `alpha_mode` / dispersion 语义审计。

下一步：
- 先补 `alpha_mode` 和 alpha semantics 的 profile/NPZ/summary/dry-run 字段。
- 仍不运行 draft profile pressure，除非完成补丁、dry-run quality 和用户明确授权。

## 2026-05-23 alpha semantics 字段传播

- [x] 生成 `alpha_semantics_propagation` evidence brief：见 `outputs/evidence_briefs/alpha_semantics_propagation/evidence_brief.md/json`
- [x] 生成执行后反馈：见 `outputs/evidence_briefs/alpha_semantics_propagation/gap_feedback.md/json`
- [x] `build_ct_acoustic_model.py` 的 NPZ alpha metadata 增加 `alpha_mode`
- [x] `simulate_kwave_3d_focus.py` 的 dry-run/model summary 增加 alpha semantics 和 `pressure_allowed_by_alpha_semantics`
- [x] 生成 dry-run：`outputs/alpha_semantics_propagation_dry_run/079_prestus_candidate006/quality_dry_run_summary.json`

验证结论：
- dry-run 中 `alpha_power=1.0`
- dry-run 中 `alpha_mode=null`
- dry-run 中 `alpha_coeff_kind=alpha0_prefactor`
- dry-run 中 `alpha_semantics_status=review_pending_requires_kWaveMedium_semantics_audit`
- `pressure_allowed_by_alpha_semantics=false`
- 本阶段未运行 k-Wave，未生成新的压力场或 CT 声学模型。

下一步如果继续 source-backed mapping，应先做 `alpha_mode/no_dispersion` 证据审查；如果不继续该方向，应回到 `simple_hu300` baseline 做 standard-review 报告。

## 2026-05-23 alpha_mode/no_dispersion 策略审查

- [x] 生成 `alpha_mode_policy_review` evidence brief：见 `outputs/evidence_briefs/alpha_mode_policy_review/evidence_brief.md/json`
- [x] 新增策略审查脚本：`review_alpha_mode_policy.py`
- [x] 输出策略报告：`outputs/alpha_mode_policy_review/policy_review.md/json`
- [x] 输出证据清单：`outputs/alpha_mode_policy_review/evidence_inventory.csv`

关键结论：
- k-wave-python 明确支持 `alpha_mode=no_dispersion`，并说明 `alpha_power=1` 的 dispersion term 不有效。
- PRESTUS 源码存在 `fit_alpha_power` 路线，默认将多组织 alpha 拟合到固定 `alpha_power=2`。
- BabelBrain 的 dispersion correction 不是 kWaveMedium `alpha_mode` 的直接证据。
- 当前推荐策略为 `keep_blocked`：不修改现有 `prestus_marsac_mueller_draft`，不运行 pressure。

下一步：
- 若继续 source-backed mapping，应先设计 dry-run-only 的两个实验 profile 方案：
  - `experimental_no_dispersion_profile`
  - `prestus_fit_alpha_power_2_profile`
- 两个方案都先做 model-build/dry-run，不直接进入 pressure。

## 2026-05-23 source-backed alpha profile 方案对照

- [x] 生成 `source_backed_alpha_profile_options` evidence brief：见 `outputs/evidence_briefs/source_backed_alpha_profile_options/evidence_brief.md/json`
- [x] 新增方案计划脚本：`plan_source_backed_alpha_profile_options.py`
- [x] 输出方案报告：`outputs/source_backed_alpha_profile_options/profile_option_report.md`
- [x] 输出 profile 草案：
  - `outputs/source_backed_alpha_profile_options/profile_drafts/prestus_marsac_mueller_no_dispersion_experimental.json`
  - `outputs/source_backed_alpha_profile_options/profile_drafts/prestus_fit_alpha_power_2_plan_only.json`

关键结论：
- no-dispersion 草案只允许 isolated model-build-only validation，不允许 pressure。
- fit-alpha-power-2 草案必须先迁移 PRESTUS `fitPowerLawParamsMulti` 公式，不能只改 `alpha_power=2`。
- 两个草案都没有放进主 `acoustic_mapping_profiles/`，避免后续误用。
- 本阶段未运行 k-Wave，未重建 CT，未生成新的压力场或模型。

下一步可选：
- 想快速推进：只对 no-dispersion 草案做一次 isolated model-build-only validation。
- 想证据更稳：先迁移并验证 `fitPowerLawParamsMulti` 公式。

## 2026-05-24 no-dispersion 草案模型构建验证

- [x] 生成 `no_dispersion_model_build_validation` evidence brief：见 `outputs/evidence_briefs/no_dispersion_model_build_validation/evidence_brief.md/json`
- [x] 对 no-dispersion 草案构建独立模型：`outputs/ct_acoustic_model_3d_profile_no_dispersion_experimental/`
- [x] 生成 model-build-only 对照报告：`outputs/no_dispersion_model_build_validation/079_no_dispersion_vs_draft/model_build_validation.md`
- [x] 生成 3D dry-run quality summary：`outputs/no_dispersion_model_build_validation/079_no_dispersion_dry_run/quality_dry_run_summary.json`

关键结果：
- `alpha_power=1.0`
- `alpha_mode=no_dispersion`
- `alpha_coeff_kind=alpha0_prefactor`
- `alpha_semantics_status=experimental_dry_run_only_no_pressure`
- `alpha_pressure_allowed=false`
- `pressure_allowed_by_alpha_semantics=false`

结论：
- no-dispersion 草案模型构建链路可用。
- 该结果仍不是 pressure validation，不得作为 validated baseline。
- 本阶段未运行 k-Wave，未生成新的压力场。

## 2026-05-24 PRESTUS fit-alpha-power=2 模型构建验证

- [x] 生成 `fit_alpha_power_migration` evidence brief：见 `outputs/evidence_briefs/fit_alpha_power_migration/evidence_brief.md/json`
- [x] 使用 `acoustic_mapping_profiles/prestus_fit_alpha_power_2.json` 构建独立 079 模型：`outputs/ct_acoustic_model_3d_profile_prestus_fit_alpha_power_2/`
- [x] 生成 no-dispersion 和 draft 两组 model-build-only 对照：`outputs/fit_alpha_power_migration/`
- [x] 生成 dry-run quality summary：`outputs/fit_alpha_power_migration/079_fit_alpha_power_2_dry_run/quality_dry_run_summary.json`

关键结果：
- `alpha_power=2.0`
- `alpha_coeff_kind=fit_alpha_power_2_prefactor`
- alpha range 约 `0.002-34.8 dB/(MHz^2 cm)`
- `alpha_pressure_allowed=false`
- `pressure_allowed_by_alpha_semantics=false`

结论：
- PRESTUS `fitPowerLawParamsMulti` 路线已完成 model-build-only 验证。
- 该 profile 仍是 `review_pending_model_build_only_do_not_use_as_default`，不是 validated pressure baseline。
- 本阶段未运行 k-Wave，未生成新的压力场。

## 2026-05-24 PRESTUS `fitPowerLawParamsMulti` 公式与单位审计

- [x] 生成 `fit_alpha_power_formula_audit` evidence brief：见 `outputs/evidence_briefs/fit_alpha_power_formula_audit/evidence_brief.md/json`
- [x] 新增审计脚本：`audit_fit_alpha_power_formula.py`
- [x] 生成公式审计报告：`outputs/fit_alpha_power_formula_audit/formula_audit_report.md/json`
- [x] 生成样本拟合表：`outputs/fit_alpha_power_formula_audit/sample_fit_table.csv`

关键结果：
- PRESTUS 源码中的 `w=2*pi*f_ref`、`db2neper`、desired absorption、二阶 denominator 和 `neper2db` 片段均通过检查。
- Python 迁移实现与 PRESTUS 公式结构一致。
- `y_ref=2` 时二阶项近似为 0，因此 fitted alpha0 与 `alpha_at_500kHz / 0.5^2` 一致。
- 这解释了 `prestus_fit_alpha_power_2` 中 skull alpha prefactor 约 `16-34.8 dB/(MHz^2 cm)` 的来源。

结论：
- 公式迁移在源码片段和数值 sanity 层面通过审计。
- 这仍不是 pressure validation，不能解除 `pressure_allowed=false`。
- 下一步建议生成 `prestus_fit_alpha_power_2` 的 entry-path property profile。

## 2026-05-24 `prestus_fit_alpha_power_2` 入射路径属性对照

- [x] 复用 `generate_entry_path_property_profile.py`，对 079 `candidate_006` 生成包含 fit-alpha-power=2 的路径属性对照。
- [x] 输出 CSV/JSON：`outputs/entry_path_property_profiles/079_candidate_006_fit_alpha_power_2/`
- [x] 新增中文解释：`outputs/entry_path_property_profiles/079_candidate_006_fit_alpha_power_2/fit_alpha_entry_path_interpretation.md`
- [x] 新增路径属性图：`path_material_profiles.png` 和 `path_alpha_by_semantics.png`

关键结果：
- 四个模型在同一路径上均识别到 `18` 个 skull 样本。
- `simple_hu300`：skull `c=2800 m/s`，`rho=1900 kg/m3`，`alpha=8 dB/(MHz cm)`。
- PRESTUS draft/no-dispersion：skull `c≈1511.5-1581.6 m/s`，`rho≈1006.8-1048.3 kg/m3`，`alpha≈17.19-17.37 dB/(MHz cm)`。
- `prestus_fit_alpha_power_2`：同样的 `c/rho`，alpha prefactor 约 `34.38-34.74 dB/(MHz^2 cm)`。

结论：
- 当前路径经过低 HU 边缘颅骨，PRESTUS route 的材料解释仍需谨慎。
- 该结果不支持直接进入 pressure；应先做路径可视化或单元交叉检查。

## 2026-05-24 MATLAB/Python 交叉测试包

- [x] 生成 `fit_alpha_power_matlab_crosscheck` evidence brief。
- [x] 生成输入样本：`outputs/fit_alpha_power_matlab_crosscheck/input_fixture.csv`
- [x] 生成 Python 期望输出：`outputs/fit_alpha_power_matlab_crosscheck/python_expected_outputs.csv/json`
- [x] 生成 MATLAB runner：`outputs/fit_alpha_power_matlab_crosscheck/run_fit_alpha_crosscheck.m`
- [x] 附带 MATLAB helper：`db2neper.m`、`neper2db.m`

当前状态：
- 本机未发现 MATLAB，因此未执行 MATLAB 端对照。
- 交叉测试包已准备好，可在有 MATLAB 的环境中运行。
- 本阶段未运行 k-Wave，未生成 pressure field。

## 2026-05-30 Source-backed alpha mapping 阶段收口

- [x] 新增 `source_backed_alpha_stage_report` evidence brief。
- [x] 新增 `generate_source_backed_alpha_stage_report.py`。
- [x] 生成阶段报告：`outputs/source_backed_alpha_stage_report/source_backed_alpha_stage_report.md`。
- [x] 生成 JSON 摘要：`outputs/source_backed_alpha_stage_report/source_backed_alpha_stage_summary.json`。
- [x] 写入执行后反馈：`outputs/evidence_briefs/source_backed_alpha_stage_report/gap_feedback.md/json`。

关键判断：

- `simple_hu300` 仍是当前可复现 baseline。
- `prestus_fit_alpha_power_2` 已完成 model-build、公式审计、路径属性对照和 MATLAB 测试包准备，但仍不是 validated pressure baseline。
- 当前发现 pressure gate 状态冲突：历史 gap feedback 记录不放行，而 dry-run summary 中存在放行字段；在该冲突解决前，按保守策略处理为 `review_pending / do not promote / no new pressure run`。
- 本阶段未运行 k-Wave，未重建 CT，未生成新的 pressure field。

下一步建议：

- 若有 MATLAB 环境，先运行 `outputs/fit_alpha_power_matlab_crosscheck/run_fit_alpha_crosscheck.m` 并导入 `matlab_outputs.csv` 做交叉误差比对。
- 若要加快主线，先冻结 source-backed alpha 为 exploratory，回到 `simple_hu300` baseline 做 standard / paper-grade 路线复核。

